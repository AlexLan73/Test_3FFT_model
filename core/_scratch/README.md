# `_scratch` — временная папка

Сюда сложено то, что не относится к тракту radar3d, но удалять жалко.
Не импортируется из `core`. Разобрать и вынести/удалить при случае.

| файл | что это |
|------|---------|
| `UpDatePip.py` | утилита: `pip list --outdated` → массовое обновление пакетов venv |
