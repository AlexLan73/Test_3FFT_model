# 📅 Сессия 2026-07-19

## Параболическое уточнение пика в кубе (ex5) — решение Alex по TASK_ex1_search_p2 §3

- **Core**: `core/models/tokenizer/peak_refine.py` — `parabolic_delta` (лог-парабола
  δ=½(P₋−P₊)/(P₋−2P₀+P₊), δ∈[−½,½], вырожденный случай → 0), `refine_peak` (N-D,
  по каждой оси независимо, край без заворота, argmax или заданный индекс из CFAR/грубой
  карты), `RefinedPeak` (VO), `axis_value_at`. Экспорт из `core.models.tokenizer`.
- **Демо**: `demo/ex5_peak_refine/` — 3 CW-цели на ДРОБНЫХ (kx, ky, f): steering
  `ArrayGrid` — честный непрерывный фазовый вектор, источники между ячейками куба.
  AmToCube 32×32×256 → топ-3 NMS → `refine_peak`. Метрики: **argmax 0.29 бина →
  парабола 0.012 (~×25 точнее)**, устойчиво на clean и SNR 0 дБ.
- **Тесты**: `tests/test_peak_refine.py` 7 ok (гаусс — восстановление δ до 1e-9,
  вырожденные случаи, край, заданный индекс, интеграция steering→AmToCube→парабола);
  `demo/tests/test_ex5.py` 4 ok. Регресс: бэкенд агрегатор зелёный, demo агрегатор зелёный.
- Регистрация: `run_all.py`, `all_demo_test.py`, `tests/all_test.py`.
- **НЕ закоммичено** (за Alex).

## ex5-web — живой прототип web-панели (требование Alex: «всё двигается, не картинки»)

- `demo/ex5_peak_refine/web.py` → `demo/graphics/ex5_peak_refine/web/index.html`:
  самодостаточный HTML+JS (vanilla canvas, 850 КБ, без внешних URL — plotly дома нет,
  работает офлайн и на Debian). Python считает 60 тактов × 2 SNR и встраивает JSON;
  браузер анимирует.
- Движение — реюз ex4: `_MODEL_BUILDERS` (5 моделей core.motion, случайный выбор) +
  `_random_initial_state` + `Kinematics` → дробные (kx, ky); дальность → несущая
  (сближение = дрейф частотного бина). Разнос целей — offsets поверх кинематики.
- Панель: угловая карта + трейлы (argmax-«лесенка» серым vs гладкая парабола оранжевым),
  зум 7×7, срезы 3 осей с параболой, график ошибки по тактам, таблица текущего такта;
  контролы: play/pause, скорость, слайдер тактов, SNR, выбор цели, трейлы.
- **Метрики: argmax 0.394 бина → парабола 0.013 (~×30), 3/3 цели на всех 60 тактах.**
- Рендер проверен headless-Edge (скриншот: карта/зум/срезы/график рисуются).
- Тесты: `Ex5WebTests` 3 ok (движение, парабола бьёт argmax, страница самодостаточна);
  demo агрегатор зелёный.

## Web-панель полёта по канону ex4 §1.6 (главное за сессию)

- Alex: v1-панель оставить, но канон — вчерашняя компоновка ex4 (3D + поле с хвостами 8
  тактов + тепловые срезы и ТАБЛИЦА признаков — «кусочек с таблицей используем»).
  Глубокий анализ → спека `specs/web_panel_flight_2026-07-19.md`; решения Alex:
  1A полный размер · 2A кроп срезов ±8 · 3B параболу в тракт потом.
- **Архитектура: web = третий рендерер history Ex4Flight** (R5): рефактор
  `run_history(rng)` (из `visualize`, GIF/PNG не тронуты) → `demo/ex4_flight/web.py`
  (`history_to_json` + самодостаточная страница, 0 внешних URL).
- Страница: 3D с вращением мышью/зумом · поле №+✈ хвосты K=8 · срезы ±8 · таблица
  §4.11 с тултипами · play/скорость/слайдер/след · deep-link `#t=N` (открывает на паузе).
- Ex4WebTests 4 ok; **demo агрегатор 10 наборов ВСЁ ЗЕЛЁНОЕ**. Смотрено глазами
  (headless-Edge, такты 1 и 6).
- **Правки Alex (красота/эргономика 3D, итерации по headless-скриншотам):**
  (1) сетка на 3 ЗАДНИХ гранях куба (выбор по глубине центра грани — matplotlib «box») +
  числовые деления на всех 3 осях + подписи; на поле ② тоже деления; дубли меток в общем
  углу пропускаются. (2) Грани ярче + периметр каждой задней грани обведён контуром.
  (3) Сетка граней — ПУНКТИР (`[3,4]`), периметр сплошной — разгрузить картинку.
  (4) **Ракурс «навстречу»**: `az=-60°, el=-20°` (было el=+18° — объект при сближении
  уходил ВВЕРХ = «от зрителя»). Теперь нулевая дальность (позиция 0) — передний нижний
  план, объекты (r убывает, приближаются) летят ВНИЗ НА зрителя. Подписи осей — на
  серединах рёбер с выносом наружу от центра куба (иначе kx-подпись уезжала за кадр).
  Ex4WebTests 4 ok на каждой итерации.

## Запуск для осмотра

```bash
.venv/Scripts/python.exe demo/ex4_flight/web.py            # ПАНЕЛЬ-КАНОН (30 тактов, ~2.5 мин)
# → открыть demo/graphics/ex4_flight/web/index.html   (deep-link: index.html#t=29)
.venv/Scripts/python.exe demo/ex5_peak_refine/example.py   # парабола: статик-PNG
.venv/Scripts/python.exe demo/ex5_peak_refine/web.py       # парабола: живая страница v1
```
