# Сессия 2026-06-22

## Сделано

- Глубокий анализ базовых проектов `DSP-GPU` и `rag-mentor`.
- Перенос и адаптация конфигурации Claude Code под radar3d:
  - `CLAUDE.md` — шапка проекта, архитектура, паттерны, запуск.
  - `.claude/rules/` — 6 правил (workflow, профиль, worktree-safety, NO-pytest, python-style).
  - `.claude/hooks/` — `pre_bash.sh` (защита + блок pytest), `post_write.sh`, `on_stop.sh`; wired в `settings.json`.
  - `.claude/settings.json` — permissions (python/git/ruff/mypy) + deny опасных + hooks.
  - `.mcp.json` — filesystem / git / sequential-thinking / memory.
  - `pyproject.toml` — метаданные + ruff/mypy + deps (numpy, matplotlib, [cnn] torch).
  - `common/runner.py` + `__init__.py` — TestRunner (замена pytest).
  - `MemoryBank/` — каркас (MASTER_INDEX, tasks, sessions, specs, changelog).
- **Багфикс**: `Core/` → `core/` (Linux ФС регистрозависима, ломала импорты).

## torch+ROCm (GPU)

- Пересоздал `.venv` на **Python 3.12** (колёса ROCm — cp312, в 3.13 не ставятся).
- Поставил **torch 2.11.0+rocm7.2** из офлайн-пака `/mnt/data/offline-debian-pack/3_python_wheels/torch-rocm/` через `--no-deps`.
- `triton-rocm` не ставил — нужен только для `torch.compile`; CNN бежит eager на ROCm.
- ✅ Проверено: `cuda.is_available()=True`, GPU **AMD Radeon RX 9070**, `train_cnn.py` обучается на `device=cuda`, smoke-тесты 3/3 (без скипов).

## Дальше

- При желании — добавить `pytorch-triton-rocm` для `torch.compile` (из AMD-репо).
