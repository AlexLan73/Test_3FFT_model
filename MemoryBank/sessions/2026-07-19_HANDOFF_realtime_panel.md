# 🤝 ПЕРЕДАЧА ДЕЛ — реалтайм-загрузка данных в панель (продолжить завтра)

> Дата: 2026-07-19 вечер · Для: следующая сессия. Сегодня закрыта сокет-панель ex4,
> следующая задача — **реалтайм-приём данных из внешнего источника**.

## ✅ Сегодня сделано (закоммичено `4d93098`, запушено в main)

Web-панель полёта ex4 переведена на **сокеты** (было — самодостаточный HTML):
- `demo/ex4_flight/server.py` — стрим тактов `meta`/`tick` через `WebSocketTransport` (порт 8765,
  90 тактов), гоняет `Ex4Flight.run_history` ОДИН раз (предрасчёт) → публикует по кругу.
- `web/` — тонкий клиент (`index.html`+`app.js` canvas + свой `msgpack.js`, ноль внешних URL).
- **Правки Alex:** правое поле = вид С НУЛЕВОЙ дальности (`Projection.field()` `az=π`, +kx вправо);
  90 кадров; сквозные номера track_id (3D·поле·теплокарты·таблица).
- Тесты зелёные: `test_camera` 6 ok, `test_ex4_server` 6 ok, `msgpack.js` (node), demo-агрегатор 10 наборов.
- Старый `web.py` → `архив/ex4_web_selfcontained_2026-07-19.zip`.
- Память: [[ex4-socket-panel]], [[ex4-field-view-from-zero-range]], [[dont-guess-measure-projection]].

## 🎯 СЛЕДУЮЩАЯ ЗАДАЧА — реалтайм-панель

**Спека готова:** `MemoryBank/specs/realtime_panel_2026-07-19.md` (подробная, по шагам).

**Суть:** сейчас данные считаются ЗАРАНЕЕ (`run_history`) и гоняются по кругу — это НЕ реалтайм,
а воспроизведение. Alex хочет: **грузить данные в реалтайм из своего источника**, панель рисует
как сейчас. Транспорт + контракт кадра + панель УЖЕ готовы — надо развязать источник от сервера.

**Решения Alex (в спеке §3):** делать варианты **1 (сырой куб)** и **2 (готовые примитивы)**,
источники **все** (Python → C++/ZMQ → файлы) — **ОТ ПРОСТОГО К СЛОЖНОМУ**. Реализация в `demo/`.

**План (спека §6):**
1. Согласовать спеку по шагам (этапы §3 + открытые вопросы §4).
2. **Этап A** (простейший, первый): `PanelPublisher.push_tick(...)` + `demo/ex4_flight/live_demo.py`
   (Python-цикл шлёт готовые примитивы с реальной паузой) — доказать реалтайм end-to-end.
3. Этап B (сырой куб → обработка на сервере, реюз тракта core), C (C++/ZMQ), D (файлы).

**Открытые вопросы к обсуждению ЗАВТРА (спека §4) — решить ДО кода:**
1. Живой край vs история (буфер: вся история/окно N тактов/пауза фиксирует).
2. ⚠️ Вариант 1: сырой куб 64×64×4096 = 64 МБ/кадр — обрабатывать на СЕРВЕРЕ, в браузер лёгкий tick.
3. Обратный канал браузер→сервер (команды) — нужен ли (сейчас WS publish-only).
4. Целевой FPS / размер куба → дроп-политика.

## 📌 Состояние на завтра

- Ветка `main`, всё запушено. Фоновый сервер ex4 на 8765 — погашен в конце сессии (перезапустить
  `.venv/Scripts/python.exe demo/ex4_flight/server.py` при нужде).
- Начать: прочитать спеку `realtime_panel_2026-07-19.md`, пройти §4 с Alex, затем Этап A.
- Правила: математика только в `core` (07), 🚫 pytest, НЕ писать в `.claude/worktrees/`.
