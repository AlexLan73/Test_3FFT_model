# Сессия 2026-07-20 — Реалтайм-панель: спека + Этап A

## Контекст двух чатов

- **Этот чат (база/demo, БЕЗ GPU):** развязка источник↔панель, публикатор, план сырья.
- **Другой чат (GPU):** обработка сырых данных на GPU. Не пересекаемся — `SceneServer`/`transport.py`/
  `generators`/`models` не трогаю. Цель сессии (Alex): «бери всё не-GPU, строй — потом добавим GPU и всё проверим».

## Сделано

### 1. Спека `specs/realtime_panel_2026-07-19.md` — §1–§4 согласованы (коммит `d187639`)
- §2.1 контракт `tick` + опц. `feats` (по-тактные признаки → таблицы без пересчёта); `sig` в `meta`.
- §2.2 боевой вариант сырьё→обработка→tick; `RawCubeSource` (Strategy).
- §3 общий тракт B·C·D (один `SignalFrontend[sig]`, ядро в core, OCP); ЛЧМ = дечирп+2 FFT (патент гл.2–3).
- §4 закрыты все 7 вопросов: два плана (сырьё consume-and-drop / `TickLog` храним всё), развязка
  producer↔consumer, `start/stop/pause`, `truth` опц., `meta` статична (апертура не меняется).
- Фикс: размер куба 64→**128 МБ** (complex64×8); худший `512×256×10000` ≈ **10 ГБ/такт** → сырьё по
  сети только малое, иначе обработка на GPU-источнике.
- §7 «архитектура целиком» (карта слоёв/паттернов/статуса).

### 2. Этап A — публикатор + демо-источник (коммит `454fa9f`)
- `core/runtime/panel_publisher.py`: `Tick` (frozen VO), `TickLog` (append-only, `deque(maxlen=cap)`,
  храним всё), `PanelPublisher` (DI на `Transport`: `start/push_meta/push_tick/republish_meta/close`).
- `demo/ex4_flight/live_demo.py`: детерминированная летящая точка → `tick` §2.1, реалтайм-цикл `time.sleep`.
- Тесты (TestRunner): `TickLogTests`/`PanelPublisherTests` (`tests/test_runtime.py`) + `LiveDemoTests`
  (`demo/tests/test_live_demo.py`); в обоих агрегаторах. Делегировано Sonnet, глубокое ревью Кодо (сам гонял тесты).
- Реалтайм доказан на уровне данных; визуалка в браузере — за Alex.

### 3. Рефактор `server.py` → PanelPublisher (коммит `2cc7621`)
- Реальный ex4-сервер переведён с инлайн-publish на `PanelPublisher` + `TickLog(cap=len(ticks))`.
- Код Этапа A теперь боевой (не только `live_demo`); `cap` не даёт бесконечному реплею растить память.
- Проверено: demo-агрегатор зелёный, `serve()` headless 1 цикл ОК.

### 4. План сырья (в работе, делегировано Sonnet) — посадочная площадка под GPU
- `core/runtime/raw_queue.py` (`RawFrame`/`RawQueue` consume-and-drop), `raw_source.py`
  (`RawCubeSource` ABC + `FileSource` Этап D), тесты, README-секция. GPU-генерация воткнётся сюда позже.

## Дальше
- Принять план сырья (ревью Кодо). Затем: хук `on_connect` (§4.4, трогает общий `transport.py` —
  согласовать с GPU-чатом), Этап B (фронтенд по `sig` + ядро — часть на GPU-чате), канал `start/stop` (§4.2).
- Задачи/статус: `tasks/TASK_realtime_panel_A.md`, `tasks/IN_PROGRESS.md`.
