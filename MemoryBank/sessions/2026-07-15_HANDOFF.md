# 🤝 ПЕРЕДАЧА ДЕЛ — body_motion (P1/P2/P5 готовы) → следующий чат: P3/P4/P6

**Дата:** 2026-07-15 · **Статус:** P1,P2,P5 ✅ приняты · тесты **105 ok/0 fail/10 skip**.

## 📖 Читать первым в новом чате
1. `MemoryBank/specs/body_motion_3d_2026-07-15/SPEC.md` — спека (финал).
2. `MemoryBank/specs/range_scale_dictionary_2026-07-15.md` — дальностные величины V1/V2/V3 (КРИТИЧНО).
3. `MemoryBank/specs/body_motion_3d_review_2026-07-15.md` — ревью F1–F10.
4. Таски `MemoryBank/tasks/TASK_body_motion_p{1..6}.md`.
5. Этот файл.

## ✅ Сделано (реализовано Sonnet-агентами, ревью Кодо)
- **P1** — фундамент: `core/config/project_config.py` (агрегат VO), `core/data_context/message_bus.py`
  (шина Observer, отдельно от Facade), `core/motion/` (`TargetState`,`MotionModel`:cv/ca/turn/
  **WeavingManeuver** манёвр+смена скорости, `Kinematics`), `TactSequence`. Демо `demo_body_motion.py`
  (+`viewer.html` — веб-3D, ГСЧ старт, дальность в горизонте).
- **P2** — `core/generators/volume.py` (`VolumeBuilder` splat цели+шум → куб 16×16×N, `iter_cubes`
  публикует в шину). Демо `demo_body_motion_volume.py`.
- **P5** — `core/generators/waveforms/waveform_to_cube.py` (`LfmToCube`/`AmToCube`),
  `core/models/range_fft.py` (`RangeFft`), `core/models/angular_fft.py`, `core/graphics/square_view.py`
  (`SquareView`+блок ±N+токены), `core/generators/waveforms/heterodyne.py` (**вендор дечирп** `rx·conj(ref)`).
  Демо `demo_body_motion_square.py` → `graphics/body_motion/p5_squares/`.

## 🔑 Ключевые решения / физика (не переоткрывать)
- **ЛЧМ тракт (патент гл.3):** цель=**задержанное эхо** `getX_numpy(tau=−2R/c)` (НЕ короткое окно!)
  → дечирп `heterodyne.dechirp` → **дальностный FFT глобально + zero-pad** `N_fft=next_pow2·(1|2|4)`,
  rect (окно по дальности НЕ до замера ширины) → угловой FFT 16×16 **+ Хэмминг обе оси** → квадрат.
- **Дальностная шкала = ТОЛЬКО V1** `=c·fs/(2μN_fft)`; ширина=Δr_eff=`c/(2μT_overlap)` (не номинал 25м);
  V3=12.5м (c/2fs) — сырой строб ДО дечирпа, как шкалу НЕ применять. Знак `f_b=−μτ` → маппинг по `|f|`.
- **Штатный визуал:** сырые кубы НЕ выводим; тепловая карта 16×16 после 2D-FFT (|·|², fftshift, Хэмминг).
  Дальность **в горизонте**. Блок ±N (дефолт 5): 5 окон с бином+расч.дальностью + квадрат с токенами.
- **Сигнатуры:** цель=точка в 1 окне · протяжённая=2–3 · гребёнка=разнесённые точки · заград=полоса
  по всей дальности (дечирпом не сжимается) · АМ=разреженные выбросы. Подтверждено визуалом P5.
- **DataContext**=шина всего обмена; визуал=Observer. Транспорт (P6)=ZMQ+MessagePack, fan-out
  (ZMQ десктоп + WebSocket браузер), **два фронта, один движок**. Хэмминг обязателен.

## 🔜 Дальше (по схеме: Кодо таск→ревью → Sonnet код → глубокое ревью Кодо)
- **P3** — помехи в СЦЕНЕ: `TASK_body_motion_p3.md`. Реюз `jammers.py`(куб-уровень, raw fast-time, A2)
  +`jammers_rf.py`. `SceneModeler` наполняет существующий `Scene`-Composite (A1, НЕ второй). Визуал —
  через P5 SquareView (заград=полоса, гребёнка=точки по дальности).
- **P4** — мультицель (Composite, `TactSequence`).
- **P6** — сокет-панель: `Transport`(ZMQ+WS fan-out)+`SceneServer`+Command + Dear PyGui + веб-дашборд
  (образец `GPUWorkLib/PyPanelAntennas`, писать СВОЁ). Закладка ±N.

## ⚠️ Гочи среды
- **ФС-рассинхрон:** запись через file-edit иногда обрезается на Linux-стороне. Писать код через
  **bash heredoc / python write** + `ast.parse` проверка после. (агенты так и делали).
- Тесты: 🚫 pytest — только `common.runner.TestRunner`. Прогон: `python3 tests/all_test.py`.
- venv Windows=cp312; в этой Linux-среде системный `python3`. GPU-часть (hip) — позже на Debian.
- НЕ git commit без OK Alex.

## Как возобновить
Поднять 5 файлов из «Читать первым» → сказать Кодо «делаем P3» → Кодо готовит/сверяет таск →
Sonnet-агент реализует (bash-запись!) → Кодо ревью (тесты + визуал квадрата + критерии словаря).
