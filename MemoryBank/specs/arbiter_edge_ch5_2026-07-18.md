# Физический арбитр гл.5 (передний край τ≥0) — реализовано (2026-07-18)

> **Схема:** Кодо разведка/ТЗ (сверка с `Doc/Patent/glava5_peredniy_krai.md`) → Sonnet код → ревью Кодо.
> **Место в конвейере:** токенизатор (гл.4) → гейт → **арбитр (гл.5)** → целеуказание FM-m (гл.8).

## Что сделано

`core/models/tokenizer/arbiter.py`:
- `TargetDecision` (VO, примитивы): `kx, ky, lead_r, decision {target|false|jammer}, reason, n_false`.
- `Arbiter(ABC)` — Strategy: `arbitrate(list[RangeVerdict]) -> list[TargetDecision]`.
- **`EdgeArbiter`** — Вариант 1 (§5.2, геометрия причинности τ≥0):
  - `target` (одиночка) → `target/single`.
  - `comb` (гребёнка ретранслятора) → **передний край `lead_r` = истинная цель** (`target/leading_edge`),
    хвостовые копии не плодятся (одна decision на группу). Проверено: r=10,20,30,40 → цель на r=10.
  - `barrage` (самоприкрытие §5.5) → `jammer/barrage_selfscreen`.
  - `angle_merge_tol=1`: джиттер-осколки `target` под НЕПОСРЕДСТВЕННЫМИ соседями (±1) заграда — поглощаются.
- **`CodeArbiter`** — Вариант 2 (§5.3, свежесть FM-m кода): задел (`NotImplementedError`), LSP-абстракция.

`tests/test_arbiter.py` (EdgeArbiterTests 6 + CodeArbiterTests 1) — зарегистрирован в `all_test.py`.
Проверено Кодо: test_arbiter 7 ok, весь бэкенд `all_test.py` 0 fail.

## ⚠️ Ограничение Варианта 1 (архитектурно, НЕ баг)

На ЧИСТОМ заграде остаётся ~93 ложных `target` (одиночные шумовые осколки под СЛУЧАЙНЫМИ углами,
разбросанные по всей карте — не ±1 соседи). Угловая толерантность гасит только прямых соседей.

**Это работа Варианта 2 (FM-m код), не Варианта 1.** По патенту §5.1 нельзя решать по яркости
(DRFM-копия может быть ярче), а по причинности τ≥0 одиночный осколок неотличим от слабой цели —
геометрия его давить не должна. §5.4: нужны ОБА варианта («геометрия ловит что упускает код, и
наоборот»). Осколок не ответит на свежий FM-m код → снимается Вариантом 2. Также §4.8: порог токена
МЯГКИЙ намеренно (передний край терять нельзя) — осколки плата за это.

## Следующий шаг

**Вариант 2 — `CodeArbiter` (FM-m код-корреляция, гл.5.3/гл.6):** реюз FM-m коррелятора генераторов
(`waveforms/fm.py`, P4). Осколок/ретранслятор со «старым» кодом не сжимается (−10·log₁₀(L) дБ) →
`false`. Замкнёт чистку осколков. Плюс трекинг между тактами (§4-бис.4) для «летит».
