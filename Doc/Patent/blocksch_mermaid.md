# Блок-схема устройства ECCM 3FFT — Mermaid (Фиг. 1)

> Редактируется текстом, рендерится вектором. Тип — `flowchart` (для блок-схем).
> Нумерация 1–20. Направление LR; вертикально — замени на `flowchart TB`.
> ВАЖНО: (1) текст узла со спецсимволами — в кавычках `B6["...: ..."]`;
> (2) после метки на стрелке `-->|...|` ставь РОВНО ОДИН пробел, иначе ошибка `got 'SPACE'`.

```mermaid
flowchart LR
    ANT(["АФАР"]) --> B2

    B1["1 · управление системой"] -->|"тип сигнала, параметры"| B2["2 · РЧ-тракт (ЦАП/АЦП, дечирп)"]
    B2 --> B3["3 · запись данных на ГПУ, Q·l"]

    B3 -->|"ЛЧМ"| B5["5 · интерфейс ЛЧМ (дальностное БПФ)"]
    B3 -->|"АМ"| B6["6 · интерфейс АМ (формирование куба)"]
    B3 -->|"ФМ"| B7["7 · интерфейс ФМ (спектр принятого)"]

    subgraph LFM["Тракт ЛЧМ"]
      B5 --> B8["8 · апертура 16×16"] --> B9["9 · угловое 2D-БПФ"] --> B10["10 · токен ЛЧМ"]
    end
    subgraph AMG["Тракт АМ"]
      B6 --> B11["11 · объёмное 3D-БПФ"] --> B12["12 · токен АМ"]
    end
    subgraph FMG["Тракт ФМ · коррелятор"]
      B7 --> B18["18 · сопряжённое перемножение"]
      B15["15 · опорный m-код"] --> B16["16 · циклические сдвиги"] --> B17["17 · спектр опор"] --> B18
      B18 --> B19["19 · IFFT"] --> B20["20 · токен коррелятора"]
    end

    B10 --> B13["13 · причинностный арбитр (τ≥0)"]
    B12 --> B13
    
    B10 -->|"трудные"| B14["14 · объёмный добор LSTM"]
    B12 --> B14

    B13 --> B4["4 · управление потоком данных"]
    B14 --> B4
    B20 --> B4

    B4 -->|"матрица токенов"| B1
    B4 -->|"управление"| B5["5 · интерфейс ЛЧМ (дальностное БПФ)"]
    B4 -->|"управление"| B6["6 · интерфейс АМ (формирование куба)"]
    B4 -->|"управление"| B7["7 · интерфейс ФМ (спектр принятого)"]
    B4 -->|"формирование опорного кода"| B15
    B4 -->|"границы области интереса"| B14

    B1 <-->|"команды"| B4
```

## Как редактировать (шпаргалка)
- **Текст узла с `:`, `,`, `·`, `×`, `≥` — только в кавычках:** `B6["6 · АМ: куб"]`.
- **После метки на стрелке — один пробел:** `B3 -->|"АМ"| B6` (два пробела = ошибка `got 'SPACE'`).
- Метку на стрелке тоже бери в кавычки, если кириллица: `-->|"команды"|`.
- Добавить блоку вход: `X --> B5`. Добавить выход: `B5 --> Y`.
- Соединить вход блока 5 с выходом блока 4: `B4 --> B5`.
- Двойная стрелка (в обе стороны): `B4 <--> B5` (с подписью `B4 <-->|"команды"| B5`).
- Линия без стрелки: `B4 --- B5`.
- Вертикальная раскладка: `flowchart TB` вместо `flowchart LR`.
