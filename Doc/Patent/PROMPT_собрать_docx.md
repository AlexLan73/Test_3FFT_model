Ты — Кодо. Рабочая папка: E:\Test_3FFT_model\Doc\Patent (запускай всё оттуда).

ЗАДАЧА: собрать в .docx только ОДИН свежий файл — формулу-якорь. Три главных .docx
(2 заявки + статья) уже актуальны, их пересобрали ранее — ТРОГАТЬ НЕ НАДО.
.md НЕ РЕДАКТИРОВАТЬ — только конвертация.

ПАРА (исходник → результат):
  formula_izobreteniya.md → Формула_3FFT.docx

ГРАБЛЯ (из брифинга): Edit/Write пишут в хост E:, а bash-mount отстаёт — свежий .md
через bash может читаться ОБРЕЗАННЫМ, и pandoc сделает битый docx.
Поэтому ПЕРЕД конвертацией проверь целостность и сверь хвост:

  python -c "s=open('formula_izobreteniya.md',encoding='utf-8').read(); print(s.count(chr(10)),'строк | хвост:',repr(s.strip()[-90:]))"

Ожидается (должно совпасть, иначе файл обрезан — СТОП, не конвертируй):
  - formula_izobreteniya.md (89 строк): '...раздельно три трека: способ, устройство, статья (отдельные документы/заявки).'

КОНВЕРТАЦИЯ (из папки Doc\Patent; картинок в якоре нет, но --resource-path=. не помешает):

  pandoc formula_izobreteniya.md -o "Формула_3FFT.docx" --resource-path=. --from gfm

Если pandoc не установлен: winget install --id JohnMacFarlane.Pandoc  (потом открой новый терминал).

ПОСЛЕ СБОРКИ:
  ls -l "Формула_3FFT.docx"    (ожидаемо десятки КБ — текст без картинок)

Отчитайся: размер файла + подтверждение, что хвост совпал.

ЧТО В ЯКОРЕ (чтобы не «чинил» обратно):
- способ — пункты 1–12 (добавлены объёмный п.10, 4 класса п.11, адаптивная нарезка п.12);
- устройство — блочная формула У.1–У.8 (блоки 1–20), зеркало zayavka_USTROJSTVO.md;
- термин FM-m «многолучевой», обозначения Q/l/k/K/S, из формулы убрана hipFFT.
Всё уже в .md — НИЧЕГО не переписывать, только собрать docx.
